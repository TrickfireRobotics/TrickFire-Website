import './Example.scss'

/**
 * An example component template for future development.
 * 
 * Use this template as a reference when documenting new components.
 * 
 * @docs
 * When documenting components, follow this structure:
 * 1. Start with a brief description of what the component does
 * 2. Add @component tag to indicate it's a React component
 * 3. Document the props object and each prop with @param
 * 4. Include @returns to describe the returned JSX
 * 5. Add an @example section showing a typical use
 * 7. Use [brackets] syntax to indicate optional parameters
 */
export const Example = () => {
    return (
        <div className='example'>
        </div>
    )
}